version: "3.8"

services:
  mysql:
    image: mysql:latest
    container_name: mysql-container
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD:rootpassword
      - MYSQL_DATABASE:mydb
      - MYSQL_USER:user
      - MYSQL_PASSWORD:password

    ports:
      - "3306:3306"
